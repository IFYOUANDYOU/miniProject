<!-- index.swan -->
<view class="container">
	<swiper interval="3000" duration="500" class="swiper-view" bindchange="swiperBind">
		<block s-for="ask,idx in asks">
			<swiper-item>
        
        <view class="idx">{{idx+1}} / {{len}}</view>
      </swiper-item>
		</block>
	</swiper>
  
</view>